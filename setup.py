from setuptools import find_packages, setup 

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Build end 2 end ml pipeline that predicts food delivery time in minutes',
    author='Ankit Kumar',
    license='',
)
